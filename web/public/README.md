Static assets served at the site root by Vite.

- **shoot.mp3** — played when the doll's head finishes turning to RED.
  Swap it for any browser-playable mp3; keep it short (1-3s). The filename
  is wired in `src/RedLightGreenLight.tsx` (`DOLL_SFX_URL`).
